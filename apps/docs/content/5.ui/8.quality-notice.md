---
title: InsightQualityNotice
description: Render only cross-cutting QueryQuality metadata.
---

```vue
<InsightQualityNotice :data="dashboard.trafficSeries.meta.quality" />
```

The component handles `approximate`, `partial`, `sampled`, `sampleRate`, `thresholded`, and
`warnings`. It intentionally ignores grain, timezone, cursor, freshness, and History Fidelity.
