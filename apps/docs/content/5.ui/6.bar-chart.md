---
title: InsightBarChart
description: Compare one selected Metric across one dimension.
---

```vue
<InsightBarChart :data="dashboard.topPages" dimension="path" metric="pageViews" />
```

`metric` and `dimension` remain explicit because they are presentation selections. Valid names are
inferred from `data` where possible. Use `formatter` for display-only formatting.
