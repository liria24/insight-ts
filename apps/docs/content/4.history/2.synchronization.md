---
title: Synchronization
description: Fetch missing or provisional ranges and commit coverage safely.
---

`history.sync({ range })` reads coverage, fetches only missing or provisional intervals, and writes
them idempotently. An interval becomes covered only after all of its Provider pages are stored
durably.

Queries use the same synchronization behavior for uncovered ranges before returning the requested
result.
