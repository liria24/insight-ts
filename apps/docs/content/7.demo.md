---
title: Demo
description: Inspect a sanitized aggregate report and the optional Vue report primitives.
---

The demo endpoint returns an aggregate report, never credentials or a provider-native response.
Local development and CI use a deterministic fixture when provider credentials are absent.

::DemoDashboard
::
